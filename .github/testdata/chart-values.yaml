# SPDX-License-Identifier: MIT

manager:
  args:
    - --garm-server=http://garm-mock.default.svc.cluster.local:8080
    - --garm-username=test
    - --garm-password=test
    - --operator-watch-namespace=
    - --operator-runner-reconciliation=false

certManager:
  enable: true

webhook:
  enable: true
